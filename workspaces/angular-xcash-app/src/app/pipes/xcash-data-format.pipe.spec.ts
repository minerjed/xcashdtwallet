import { XcashDataFormatPipe } from './xcash-data-format.pipe';

describe('XcashDataFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new XcashDataFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
