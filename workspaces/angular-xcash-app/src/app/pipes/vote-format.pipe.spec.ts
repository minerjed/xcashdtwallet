import { VoteFormatPipe } from './vote-format.pipe';

describe('VoteFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VoteFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
