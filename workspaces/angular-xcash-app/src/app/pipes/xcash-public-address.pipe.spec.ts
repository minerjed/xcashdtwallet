import { XcashPublicAddressPipe } from './xcash-public-address.pipe';

describe('XcashPublicAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new XcashPublicAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
