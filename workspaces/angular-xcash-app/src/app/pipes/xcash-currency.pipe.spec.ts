import { XcashCurrencyPipe } from './xcash-currency.pipe';

describe('XcashCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new XcashCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
